import { Box, Typography } from "@mui/material";

const Hero = () => {
  return (
    <Box width={"100%"} height={800}>
      <Typography>Hero Section</Typography>
    </Box>
  );
};

export default Hero;
